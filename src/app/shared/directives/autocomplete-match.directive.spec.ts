import { AutocompleteMatchDirective } from './autocomplete-match.directive';

describe('AutocompleteMatchDirective', () => {
  it('should create an instance', () => {
    const directive = new AutocompleteMatchDirective();
    expect(directive).toBeTruthy();
  });
});
